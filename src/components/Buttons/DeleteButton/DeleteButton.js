import React from "react";
import { Button } from "@mui/material";

const DeleteButton = () => {
  <Button variant="contained" color= "danger.main" type="submit">
    Editar
  </Button>
}

export default DeleteButton;