import React from "react";
import { Button } from "@mui/material";

const SaveButton = () => {
  <Button variant="contained" color= "secondary.main" type="submit">
    Editar
  </Button>
}

export default SaveButton;